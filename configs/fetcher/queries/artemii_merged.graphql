query {
  repository(owner: "$owner", name: "$name") {
    pullRequests(states: MERGED, first: 100) {
      nodes {
        number
        title
        author { login }
        mergedAt
        createdAt
      }
    }
  }
}